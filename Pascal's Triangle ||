class Solution {
public:
    vector<int> getRow(int rowIndex) {
        vector<int> row(rowIndex + 1, 1);
        for (int i = 1; i < rowIndex; i++) {
            long long value = (long long)row[i - 1] * (rowIndex - i + 1);
            row[i] = value / i;
        }
        return row;
    }
};
